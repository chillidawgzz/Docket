That is aweseome! BUT it appears only messges lavelled with  inbox got
  sync'd. Can we sync all labelled messages as well? Also, can we add
  details to a sqlite DB so we don't have to sync every time? instead we
  can have a sync button. I'd also like a live progress on the UI instead
  of having to call the api for it and hit refresh.
